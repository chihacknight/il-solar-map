version: '2.4'
services:
  jekyll:
    build: .
    volumes:
      - ./:/usr/local/src
    working_dir: /usr/local/src
    ports:
      - 5001:3000
    environment:
      - JEKYLL_ENV=development
    entrypoint: bundle exec jekyll
    command:
      - serve
      - -w
      - --host=0.0.0.0
